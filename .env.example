# ArenaOne Environment Variables Template

# =============================================================================
# Image Storage and Serving Configuration
# =============================================================================

# Base URL for serving images (REQUIRED for production)
# This should be the public URL where Nginx serves static assets
# Examples:
#   Production:  https://arenaone.app
#   Development: https://dev.arenaone.app
#   Local:       http://localhost:8080 (with Nginx)
NEXT_PUBLIC_ASSETS_BASE_URL=https://dev.arenaone.app

# Server-side storage path for production images (REQUIRED for production)
# This is the filesystem path where images are stored in the Docker container
IMAGE_UPLOAD_PATH_PROD=/app/storage/images

# Server-side storage path for development images (REQUIRED for development)
# This is the filesystem path where images are stored in development
IMAGE_UPLOAD_PATH_DEV=/path/to/local/storage/images

# =============================================================================
# Database Configuration
# =============================================================================

DATABASE_URL=postgresql://user:password@localhost:5432/arenaone
SHADOW_DATABASE_URL=postgresql://user:password@localhost:5432/arenaone_shadow

# =============================================================================
# Authentication (NextAuth.js)
# =============================================================================

NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# =============================================================================
# Application Configuration
# =============================================================================

NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# Payment Providers (Optional)
# =============================================================================

# Google Maps API Key (for club location embedding)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Add your other environment variables here
# ...
