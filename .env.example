# Environment Variables Example
# Copy this file to .env.local and fill in your values

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/padel_platform"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# Supabase (for file storage)
NEXT_PUBLIC_SUPABASE_URL="your-supabase-url"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"

# Google Maps API (optional)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key"

# Mock Data (Development Only)
# Enable mock data for admin pages to view full UI states without backend dependencies
# Set to "true" to enable, or omit/set to "false" to disable (default: false)
# See src/mocks/README.md for details
# NEXT_PUBLIC_USE_MOCKS="true"
