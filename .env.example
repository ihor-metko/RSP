# ArenaOne Environment Variables

# =============================================================================
# Database Configuration
# =============================================================================
DATABASE_URL="postgresql://user:password@localhost:5432/arenaone"

# =============================================================================
# NextAuth Configuration
# =============================================================================
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"

# =============================================================================
# Image Upload Configuration
# =============================================================================

# Production storage path (used in Docker container)
IMAGE_UPLOAD_PATH_PROD="/var/www/arenaone_storage"

# Development storage path (used in local development)
IMAGE_UPLOAD_PATH_DEV="./uploads"

# Assets base URL for serving images
# Production: Set to your domain (e.g., https://arenaone.app)
# Development: Can be left unset to use relative paths
# If unset, falls back to relative paths like /uploads/...
NEXT_PUBLIC_ASSETS_BASE_URL="https://arenaone.app"

# =============================================================================
# Application Configuration
# =============================================================================
NODE_ENV="production"

# =============================================================================
# Notes
# =============================================================================
# 
# Image Storage:
# - Files are stored at: {IMAGE_UPLOAD_PATH}/{entity}/{entityId}/{filename}
# - URLs are generated as: {NEXT_PUBLIC_ASSETS_BASE_URL}/uploads/{entity}/{entityId}/{filename}
# - Nginx serves files from /uploads/ path directly from the storage volume
#
# For local development:
# - Copy this file to .env.local
# - Update DATABASE_URL and other settings
# - NEXT_PUBLIC_ASSETS_BASE_URL can be left unset for relative paths
#
# For production:
# - Copy this file to .env.production
# - Update all values with production settings
# - Ensure NEXT_PUBLIC_ASSETS_BASE_URL matches your domain
#
